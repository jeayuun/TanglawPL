int main() {
    // Declare variables
    int a = 5;
    double b = -3.14;
    String text = "Hello, World!";
    char grade = 'A';
    boolean isActive = true;

    /* Perform arithmetic operations */
    a = a + 10 - b * 2 / 4;

    // Comparison and logical operations
    if (a > 10 && b < 0) {
        System.out.println("Conditions met!");
    }

    // Reserved words and function calls
    fetch areaOf circle radius 5;
    return 0;
}
